<style>
    .header {
    color: white;
    background-color: rgb(33, 33, 33, 0.8);
    height: 10vh;
    }
    .userHeader {
    display: flex;
    @* justify-content: space-around; *@
    align-items: baseline;
    }
    .userHeader p {
        margin-right: 20px;
        font-size: 22px;
    }
</style>

<nav class="header navbar fixed-top navbar-expand-lg navbar-light">
            <div class="container-fluid">
                <a class="navbar-brand" href="/" style="color:white;font-size:35px"><i class="fa fa-video" style="margin-right: 10px"></i>CinePhil</a>
                <div class="collapse navbar-collapse" id="navbarSupportedContent">
                    <ul class="navbar-nav me-auto mb-2 mb-lg-0">
                        @* <li class="nav-item">
                            <a class="nav-link active" aria-current="page" href="/" style="color:white;margin-right:20px">Home</a>
                        </li> *@
                        <li class="nav-item">

                    @* <form class="d-flex"> *@
                        <input class="form-control me-2" type="search" placeholder="Search" aria-label="Search" style="width: 230px;margin-left:20px">
                        @* <button class="btn btn-outline-success" type="submit">Search</button> *@
                        </li>
                        
                    @* </form> *@
                        <li class="nav-item"><a href="https://www.themoviedb.org" style="margin-left:25px">
                            <img src="/img/blue_square.svg" alt="tmdbIcon" style="width:60px">
                            </a> </li>
                    </ul>
                    <div class="justify-content-end collapse navbar-collapse">
                        <ul class="navbar-nav">
                        <li class="nav-item">
                            @* replace with the movieAPI icon *@
                            @if(ViewBag.currentUser == null)
                            {
                            <a href="/login" class="btn btn-primary">SIGN IN</a>
                            }
                            else
                            {
                                <div class="userHeader">

                                <p>Welcome, @ViewBag.currentUser.FirstName!</p>
                                <a href="/logout" class="btn btn-danger">LOG OUT</a>
                                </div>
                            }
                        </li>
                    </ul>
                    </div>
                </div>
            </div>
        </nav>